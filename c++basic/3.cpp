//write a  c program to enter length and breadth of a rectangle and find its perimeter;
#include <iostream>
using namespace std;

int main()
{
    int l, b;
    cout << "Enter the length and bradth of recatanle" << endl;
    cin >> l >> b;
    cout << "the perimeter of a triangle is " << l * 2 + b * 2;
}