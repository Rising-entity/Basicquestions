// write a c++ program to enter a number and calculate its sq root
#include <iostream>
#include<math.h>

using namespace std;

int main()
{ int num;
    cout<<"enter the number :";
    cin>>num;
    int sqroot= sqrt(num);
    // or
    // pow(num.0.5);
    cout<<sqroot;

    return 0;
}
